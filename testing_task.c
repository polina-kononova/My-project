#include <stdio.h>
#include <stdlib.h>


void create_massiv(size_t N, ) {
  int size_massiv;
     massiv = (double **)calloc(N, sizeof(double *));
    for (size_t i = 0; i < N; i++) {
      size_massiv = rand();
      m.matrix[i] = (double *)calloc(size_massiv, sizeof(double));
    }
  }
  return m;
}

int main () {
    double** massiv;



}

